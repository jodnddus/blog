import styled from "styled-components";

const PageMoveLink = styled.a`
  text-decoration: none;
  color: black;
  padding: 0.2rem;
  &:hover {
    text-decoration: underline;
  }
`;

export default PageMoveLink;